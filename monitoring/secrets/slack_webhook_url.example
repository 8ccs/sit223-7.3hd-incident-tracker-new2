# Copy this file to slack_webhook_url.txt (gitignored) and replace the
# line below with your own real Slack Incoming Webhook URL. Get one
# from https://api.slack.com/messaging/webhooks -- it looks like
# https://hooks.slack.com/services/<TEAM_ID>/<BOT_ID>/<SECRET>
#
# Deliberately NOT written in that exact shape here, so this placeholder
# file itself can never be mistaken for (or scanned as) a real credential:
REPLACE_THIS_WHOLE_LINE_WITH_YOUR_REAL_SLACK_INCOMING_WEBHOOK_URL
